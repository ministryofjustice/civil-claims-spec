# config/cucumber.yml
##YAML Template
---
default: features --format pretty --require support --tags ~@wip ENV=development
guard:  --strict  --format pretty --require support --tags ~@wip
demo: features    --format pretty --require support --tags ~@wip ENV=demo
list_tags: features --dry-run --format Cucumber::Formatter::TagList --require support
kamikaze: features --format pretty --require support